[package]
name = "rushfetch"
version = "0.1.1"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
colored = "2.1"
libc = "0.2"
unicode-width = "0.1"

[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
strip         = true
